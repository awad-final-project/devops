# Backend Environment Configuration
# Copy this to /opt/backend/.env and update values

# ============================================
# MongoDB Configuration
# ============================================
# Choose ONE option below:

# Option 1: Local MongoDB (running in Docker)
MONGO_URI=mongodb://admin:your_mongo_password@mongo:27017
MONGO_DB=user_registration

# Option 2: External MongoDB (e.g., MongoDB Atlas)
# Uncomment and use this format instead:
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/user_registration?retryWrites=true&w=majority
# MONGO_DB=user_registration

# Note: If using Atlas, comment out the mongo service in docker-compose.prod.yml

# JWT Configuration
JWT_SECRET=CHANGE_THIS_TO_VERY_STRONG_SECRET_KEY_IN_PRODUCTION

# CORS & Frontend
CORS_ORIGIN=https://mail.nguyenanhhao.site
FRONTEND_URL=https://mail.nguyenanhhao.site

# Google OAuth (Optional - if using Google Sign-In)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=https://mail.nguyenanhhao.site/api/auth/google/callback

# Application
PORT=3000
NODE_ENV=production
